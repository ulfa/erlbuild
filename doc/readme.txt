
Note: 

in the env section the following entries can be present

timer_clients : modules which implements the interface time_triggered/1
svn_dir : directory where the svn checkout will be found
timer_interval : Time interval in msecond to the next run 
files_regex : here you can configure the cc_file_poller which kind of file he has to poll
project_dir : this is root dir of the project
polling_dir : which dir is interessting for the file poller